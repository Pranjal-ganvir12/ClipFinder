[build]
  base = "clipfinder/frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/api/*"
  to = "https://clipfinder-2jir.onrender.com/api/:splat"
  status = 200
  force = true
